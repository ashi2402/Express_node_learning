<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
    <script type="text/javascript">

        /*  $(document).ready(function() {

            // process the form
            $('#updateUserData').submit(function(event) {

                // get the form data
                // there are many ways to get this data using jQuery (you can use the class or id also)
                var formData = {
                    'empnumber'              : $('input[name=empnumber]').val(),
                    'name'             : $('input[name=name]').val(),
                    'contact'    : $('input[name=contact]').val(),
                    'department'    : $('input[name=department]').val(),
                };

                // process the form
                $.ajax({
                    type        : 'POST', // define the type of HTTP verb we want to use (POST for our form)
                    url         : '/updateData', // the url where we want to POST
                    data        : formData, // our data object
                    dataType    : 'json', // what type of data do we expect back from the server
                    encode          : true,
                    success  : function (data) {
                        alert(data);
                        console.log(data);
                    }
                });

                // stop the form from submitting the normal way and refreshing the page
                event.preventDefault();
            });

        });*/
    </script>
</head>
<body class="container">

<header>
    <% include ../partials/header %>

</header>
<body>


<form id="updateUserData" action="/updateData" method="post" class="form-horizontal" style="width: 75%">
    <% detail.forEach(function (data) {
    %>

    <div class="form-group">

        <label class="control-label col-md-2" for="EmpID">Employee ID</label>
        <div class="col-md-10">
            <input type="text" class="form-control" placeholder="ID" name="empnumber" readonly="readonly" value="<%= data.empnumber%>">
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-md-2" for="name">Name</label>
        <div class="col-md-10">
            <input type="text" class="form-control" placeholder="Name" name="name" value="<%= data.name%>">
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-md-2" for="EmpID">Contact</label>
        <div class="col-md-10">
            <input type="number" class="form-control" placeholder="Contact" name="contact" value="<%= data.contact%>">
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-md-2" for="EmpID">Department</label>
        <div class="col-md-10">
            <input type="text" class="form-control" placeholder="Department" name="department" value="<%= data.department%>">
        </div>
    </div>
    <% }); %>
    <button type="submit" value="submit">Update</button>
</form><br><br>

</body>

<footer>
    <% include ../partials/footer %>
</footer>

</body>
</html>